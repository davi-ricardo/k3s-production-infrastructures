apiVersion: v1
kind: Service
metadata:
  name: grafana
spec:
  type: NodePort
  selector:
    io.kompose.service: grafana
  ports:
    - name: "3002"
      protocol: TCP
      port: 3002
      targetPort: 3000
      nodePort: 30002