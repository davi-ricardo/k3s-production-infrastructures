apiVersion: v1
kind: Service
metadata:
  name: rustdesk-api
spec:
  type: NodePort
  selector:
    app: rustdesk-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 3000
