apiVersion: v1
kind: Service
metadata:
  name: rustdesk-frontend
spec:
  type: NodePort
  selector:
    app: rustdesk-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
