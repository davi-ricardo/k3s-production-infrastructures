apiVersion: v1
kind: Service
metadata:
  name: zabbix-server
spec:
  type: ClusterIP
  selector:
    app: zabbix-server
  ports:
    - name: trapper
      protocol: TCP
      port: 10051
      targetPort: 10051